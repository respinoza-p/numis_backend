# =============================================================================
# Paises API — Perfil DESARROLLO (dev)
# =============================================================================

# MongoDB local o Atlas de desarrollo
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://espinozaplazarodrigo:iqLOKaV7ZAutdhFR@cluster0.gip4i.mongodb.net/numismatica}
spring.data.mongodb.database=numismatica

# Logging
logging.level.root=INFO
logging.level.com.numismatica=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG

# JWT (token expira en 24 horas en dev)
app.jwt.expiration-ms=86400000

# CORS (permitir localhost en dev)
app.cors.allowed-origins=http://localhost:4200,http://localhost:3000

# Swagger habilitado en dev
springdoc.swagger-ui.enabled=true
springdoc.api-docs.enabled=true

# Actuator más permisivo en dev
management.endpoints.web.exposure.include=health,info,env,beans,mappings
